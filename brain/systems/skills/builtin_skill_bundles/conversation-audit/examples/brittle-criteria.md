# Brittle Criteria Example

A run created and pushed a branch, but the verifier only accepted a PR URL. Treat this as an evidence warning if the user outcome exists through equivalent evidence, then fix the criteria to accept branch, commit, PR, or explicit no-PR rationale as separate artifact classes.
