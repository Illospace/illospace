# Domain-Backed App

Use a Domain first when the app manages contacts, companies, deals, tasks, logs, inventory, or any records the user will sort, query, relate, or reuse outside this UI. Build the UI after the schema exists.
