{
	email {$ILLO_ADMIN_EMAIL}
}

{$ILLO_DOMAIN} {
	encode zstd gzip

	@api path /api /api/* /ws /ws/* /static/uploads /static/uploads/*
	handle @api {
		reverse_proxy api:8000
	}

	handle {
		reverse_proxy web:8080
	}
}
