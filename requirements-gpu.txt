# Optional local CPU/GPU embedding and local LLM runtime.
# Install with: pip install -r requirements-gpu.txt
#
# The recommended production Compose image intentionally does not install these
# packages, because the smooth team-server path uses EMBEDDING_BACKEND=api and
# Torch/CUDA makes first deploys much slower and larger.
sentence-transformers>=5.0
transformers>=5.0
accelerate>=1.0

# CUDA wheels are published for Linux x86_64. Other platforms should use the
# normal torch dependency resolved by the packages above or install a
# platform-specific build manually.
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.10; platform_system == "Linux" and platform_machine == "x86_64"
